<view>
  <map id="map" setting="{{mapsetting}}" longitude="{{longitude}}" latitude="{{latitude}}" markers="{{markers}}" polyline="{{polyline}}" style="height:{{mapHeight}}px;width:{{mapWidth}}px"></map>
</view>